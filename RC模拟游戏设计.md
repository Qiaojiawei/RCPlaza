# RC Plaza — 完整游戏设计方案（Windows 性能优先版）

> **实现状态（2026-09-16）**：本方案已按文档整体实现并通过自动化验收
> （EditMode 9 + PlayMode 13 = 22/22，退出码 0）。实现阶段对个别参数与技术
> 选型做了物理一致性修订——悬架 k/c（§3.3）、扭矩曲线（§3.4）、§7②③ 的
> 验收数值等共六处，且物理/音频/输入均为零依赖自研替代（§6.2–6.4 所列 SDK
> 未引入）。修订明细、最终参数与实测验收数据见文末【附录 A：实现刷新记录】，
> 冲突处以附录为当前有效口径。

## 一、项目概述

**游戏名称**：RC Plaza  
**类型**：RC 遥控车物理模拟 / 休闲驾驶  
**平台**：Windows PC（键鼠操控），原生可执行文件  
**核心体验**：在小区广场环境中，以键盘操控真实参数还原的 RC 遥控车，体验接近实车的加速、转向、悬架动作与电池动力衰减。游戏不追求“爽快感”，而是追求“手感像真车”——每一次油门变化、每一次转向入弯，都能通过物理反馈让玩家感知到 RC 车的轻量车身、高推重比和电池动力系统的真实响应。

**设计原则**：性能优先。所有技术选型、物理配置、音频方案均以 Windows 平台流畅运行为第一目标，在保证物理精度的前提下最大化帧率稳定性和 CPU/GPU 占用效率。

---

## 二、车型选择与参数体系

### 2.1 车型 A：Traxxas Slash 4X4 VXL（公路 / 短卡型）

Slash 是 RC 领域最知名的短卡车型，操控中性、重心适中，适合作为“入门公路车”选项。采用 1/10 比例版本参数：

| 参数项 | 数值 | 备注 |
|---|---|---|
| 全长 | 568 mm | 1/10 比例 |
| 全宽 | 296 mm（前 / 后轮距） |  |
| 全高 | 193 mm |  |
| 轴距 | 324 mm | 由 Traxxas 官方规格推算 |
| 整车质量（RTR） | 2.64 kg | 含电池与电子设备 |
| 动力系统 | Velineon 3500 无刷电机 + VXL-3s 电调 | 传感器less 无刷 |
| 电池配置 | 2S / 3S LiPo（默认 2S） |  |
| 最高速度 | 约 50 km/h（2S）/ 80+ km/h（3S） | 官方宣称 30+ mph，双电池 50+ mph |
| 传动比（总） | 7.32:1（出厂配置） | 内齿比 5.04:1 |
| 差速器 | 密封式钢伞齿差速 | 前 / 中 / 后三差速 |
| 悬架形式 | 四轮独立，油压避震 |  |
| 前 / 后避震长度 | 48 mm | 行程约 35 mm |
| 轮胎 | Slash All-Terrain，预粘合 | 软质橡胶，中等抓地 |

**推荐场景**：广场平整铺装路面。Slash 的短卡造型与偏公路调校的悬架，使其在光滑地面上的转向响应干净、侧倾幅度可控。

### 2.2 车型 B：ARRMA Kraton 6S BLX（越野 / 大脚型）

Kraton 是 1/8 比例电动大脚车的标杆产品，以强悍的动力和越野通过性著称。适合作为“进阶越野车”选项。

| 参数项 | 数值 | 备注 |
|---|---|---|
| 全长 | 594 mm | 1/8 比例 |
| 全宽 | 461 mm |  |
| 全高 | 195 mm |  |
| 轴距 | 378 mm |  |
| 最小离地间隙 | 33 mm | 比 Slash 更高 |
| 整车质量（RTR） | 4.85 kg | 含 6S 电池 |
| 动力系统 | Spektrum Firma 2050Kv 无刷电机 + 150A 电调 | 4S / 6S 兼容 |
| 电池配置 | 4S 或 6S LiPo |  |
| 最高速度 | 96 km/h（6S，选配高速齿比） | 官方 65+ mph |
| 传动比 | 可调（电机齿更换） | 出厂偏扭力配置 |
| 差速器 | 前 / 中 / 后，金属齿轮 | 硅油填充，可调粘度 |
| 悬架形式 | 四轮独立，大行程油压避震 |  |
| 离地间隙 | 33 mm | 适应广场边缘、路沿石 |
| 轮胎 | dBoots Copperhead 2 | 大颗粒越野胎，铺装路面抓地力偏低 |

**推荐场景**：广场铺装路面与边缘绿化带、砖缝、缓坡的混合地形。Kraton 的重量（4.85 kg vs Slash 的 2.64 kg）和更大的悬架行程带来完全不同的驾驶节奏——加速时车身后蹲更明显，刹车时前倾幅度更大，转向时需要更早地修正方向。

---

## 三、物理仿真模型

### 3.1 轮胎 - 地面摩擦模型

RC 车质量轻（1–5 kg 量级）、轮胎软、推重比极高，因此轮胎摩擦特性对驾驶手感的影响远大于真实汽车。采用基于 **Pacejka 魔术公式** 的轮胎模型。

**关键参数设计**：

| 地面材质 | 静摩擦系数 COF | 滚动阻力系数 | 适用车型 |
|---|---|---|---|
| 干燥广场砖 | 0.55–0.65 | 0.010–0.015 | 两者 |
| 水泥 / 混凝土铺装 | 0.50–0.60 | 0.010–0.018 | 两者 |
| 沥青（广场外围道路） | 0.60–0.70 | 0.010–0.018 | 两者 |
| 草地 / 绿化带 | 0.35–0.45 | 0.025–0.035 | 仅 Kraton 可勉强通过 |

广场砖的干燥静摩擦系数通常在 0.5 以上即为安全，实测值可达 0.9 以上。但 RC 车的轮胎气压极低、胎面窄，实际可用抓地力显著低于全尺寸汽车。建议以 0.55–0.65 作为广场砖的基准 COF，Slash 的公路胎在该表面上有较好的横向抓地，而 Kraton 的越野胎横向抓地降低约 20–25%。

滚动阻力方面，良好沥青 / 混凝土路面的滚动阻力系数约为 0.010–0.018。Kraton 的越野胎滚阻应设为 Slash 公路胎的 1.8–2.2 倍。

### 3.2 质量、惯量与重心

对于 RC 车这种重心极低、质量极轻的物体，力的施加点应设为略低于车身重心位置，以模拟真实的重量转移效应。

| 车型 | 整车质量 | 簧下质量（每轮） | 重心高度（估算） |
|---|---|---|---|
| Slash 4X4 | 2.64 kg | 约 0.12 kg | 55–65 mm |
| Kraton 6S | 4.85 kg | 约 0.18 kg | 65–75 mm |

Kraton 的质量几乎是 Slash 的两倍，重心略高，因此在急加速和重刹车时，车身俯仰姿态的变化幅度会明显大于 Slash。这是两款车驾驶手感差异的重要来源，应在参数中刻意保留。

### 3.3 悬架系统

**Slash 4X4**：

- Suspension Distance：0.035 m（35 mm 有效行程）
- Spring（弹簧刚度）：约 28000–32000 N/m
- Damper：约 2200–2600 N·s/m
- Target Position：0.5（中性位置）

**Kraton 6S**：

- Suspension Distance：0.045 m（45 mm 有效行程，越野调校更长）
- Spring：约 35000–40000 N/m（支撑更大的车身质量）
- Damper：约 2800–3200 N·s/m
- Target Position：0.5

RC 车的悬架行程相对于车身尺寸而言比例很大——Slash 的 35 mm 行程相当于车长的 6% 以上。因此悬架动作在视觉上非常显著，过砖缝、路沿石或加速 / 刹车时，车身姿态的变化是玩家感知“这车是真的”的第一来源。

### 3.4 动力传动与电机扭矩曲线

**Slash 4X4** 采用 Velineon 3500Kv 无刷电机。无刷电机的扭矩输出特性为：低速时扭矩较高但受电调电流限制，中速区间扭矩平台，高速时扭矩因反电动势而衰减。建议使用三段式扭矩曲线：

- 0–25% 转速区间：扭矩从 0 线性上升至峰值
- 25–70% 转速区间：扭矩保持在峰值的 85–95%
- 70–100% 转速区间：扭矩线性衰减至峰值的 35–40%

**Kraton 6S** 采用 2050Kv 电机。在 6S 锂电（22.2V）下，理论空载转速为 2050 × 22.2 ≈ 45510 RPM，远高于 Slash 3500Kv × 2S（7.4V）的 25900 RPM。扭矩曲线形态类似，但峰值扭矩应设为 Slash 的 2.5–3.0 倍。

**传动效率**：RC 车的齿轮传动效率通常在 85–92% 之间。最终轮上扭矩 = 电机扭矩 × 总传动比 × 传动效率。

### 3.5 电池动力系统模拟

锂聚合物电池在放电过程中电压会持续下降，直接影响电机 RPM 和扭矩输出：

- **满电**：每节电芯 4.2V。Slash 2S = 8.4V，Kraton 6S = 25.2V
- **标称**：每节 3.7V。Slash 2S = 7.4V，Kraton 6S = 22.2V
- **截止**：每节 3.2–3.3V

游戏中模拟电压随剩余容量和放电电流的变化：高电流放电时电压跌落更明显（电压 sag）。当电池电压下降时，电机可用 RPM 降低，动力输出衰减。

**电池容量与续航**：Slash 标配 4200mAh 2S LiPo，续航约 10–15 分钟。Kraton 使用 5000mAh 6S 电池，续航约 8–12 分钟。当电压跌至 3.4V/cell 时电调进入低压保护，动力输出限制至 50%。

---

## 四、声音设计

RC 车的声音核心声源包括：

1. **无刷电机 PWM 啸叫**：电调以 8–12 kHz 的 PWM 频率驱动电机，产生高频音调，其频率随油门开度变化。这是 RC 车最具辨识度的声音。
2. **齿轮啮合噪音**：电机齿与主减速齿轮的啮合产生连续的“呲呲”声。金属齿轮比塑料齿轮声音更大、更尖锐。
3. **轮胎滚动噪音**：软质橡胶轮胎在硬质铺装路面上滚动时产生低沉的“嗡嗡”声。
4. **风噪与车身共鸣**：高速时车壳内的空气流动产生低沉的风噪。

**实现方案**：三层音频采样叠加——电机 PWM 层（频率映射至转速）、齿轮层（音量映射至油门）、轮胎层（音量映射至速度与地面材质）。地面材质切换时，轮胎层采样在 0.15–0.25 秒内交叉淡化。

---

## 五、场景与环境

**小区广场** 的设计应包含三类地表：

- **A. 主铺装区**：大面积广场砖或混凝土铺装，平整度好。砖缝在视觉上可见，但在物理上处理为近似均匀表面，仅在有明显高低差的路沿处设置碰撞体。
- **B. 过渡区**：广场边缘的沥青或水泥路沿，高度 3–5 cm。Kraton 可以较为轻松地爬上，Slash 需要一定角度和速度才能通过。
- **C. 边缘地形**：广场周围的绿化带边缘、碎石或泥土小径。Slash 进入后明显颠簸、抓地力骤降，Kraton 的越野胎和长行程悬架在此表现出通过优势。

场景中可添加少量静态装饰物（长椅、花坛、灯柱）作为碰撞参考，但不设置“障碍赛道”——核心玩法是自由驾驶与物理感知。

---

## 六、技术实现方案（Windows 性能优先）

### 6.1 引擎与物理（性能优化配置）

采用 **Unity 2022 LTS**，目标平台为 **Windows Standalone (x86_64)**。

**物理引擎性能设置**：

| 设置项 | 推荐值 | 说明 |
|---|---|---|
| Fixed Timestep | 0.01（100Hz） | 高于默认 0.02，提升高速操控稳定性，RC 车加速度极高需要更高物理频率[reference:0] |
| Maximum Allowed Timestep | 0.033（30fps） | 限制最坏情况下物理计算耗时，防止帧率骤降时物理爆炸[reference:1] |
| Simulation Mode | Fixed Update（默认） | 使用引擎默认的固定步长仿真模式 |
| CCD（连续碰撞检测） | 启用 | 防止高速行驶时穿透墙体，RC 车极速接近 100 km/h 时需要 |
| Rigidbody 休眠 | 启用 | 车辆静止时自动进入休眠，减少物理计算[reference:3] |

**物理步长优化原则**：固定时间步长是车辆物理稳定性的基础。可变时间步长（如直接使用 Time.deltaTime）会导致物理模拟不稳定，在低帧率时出现穿透或抖动[reference:4]。RC 车加速度极高（0–100 km/h 仅需 2–3 秒），物理频率不足会导致轮胎力计算失真。

**Rigidbody 性能优化**：

- 车辆 Rigidbody 的 `centerOfMass` 设为车身底部附近，提升稳定性
- 启用 `IsSleeping` 检测，车辆静止 1–2 秒后自动休眠
- 使用 `Rigidbody.maxAngularVelocity` 限制极端旋转，防止物理爆炸

### 6.2 免费开源物理 SDK 选型

基于性能优先原则，以下开源方案均可直接用于 Windows 平台。

#### （1）TLabVehiclePhysics（推荐首选）

TLabVehiclePhysics 是一个开源的 WheelCollider 替代方案，使用 Pacejka 和 LUT（查找表）作为最小物理参数单元，通过滑移角、滑移率等参数计算物理量。

| 项目 | 详情 |
|---|---|
| 授权 | 开源 |
| 引擎要求 | Unity 2022.3.19f1 + URP |
| 核心功能 | Pacejka 轮胎模型、LUT 参数系统、扭矩曲线编辑 |
| 与 RC Plaza 的匹配度 | Pacejka 模型直接满足轮胎纵向 / 横向力计算需求；LUT 系统便于为 Slash 和 Kraton 分别配置不同的轮胎特性曲线 |
| GitHub | https://github.com/TLabAltoh/TLabVehiclePhysics |

**适用场景**：作为主物理方案。其 Pacejka + LUT 架构与本方案第三章的轮胎摩擦模型设计完全对应。

#### （2）CustomWheelCollider

CustomWheelCollider 是一个自定义车轮碰撞器实现，包含悬架和轮胎两大组件，使用 Pacejka 轮胎模型计算纵向和横向轮胎力[reference:6]。

| 项目 | 详情 |
|---|---|
| 授权 | 开源 |
| 核心功能 | 悬架 + 轮胎双组件、Pacejka 轮胎力计算、可配置驾驶动态 |
| 与 RC Plaza 的匹配度 | 可配置性使其适合调校 Slash（公路）与 Kraton（越野）两种截然不同的驾驶动态 |
| GitHub | https://github.com/sali9213/CustomWheelCollider |

**适用场景**：作为备选或参考实现。

#### （3）UDrive

UDrive 支持可配置的传动系统模拟（EV 和自定义引擎类型）、完全自定义的摩擦和悬架、3D 车轮碰撞、多地面类型。

| 项目 | 详情 |
|---|---|
| 授权 | GNU LGPL 3.0 |
| 核心功能 | EV 引擎类型、自定义摩擦和悬架、3D 车轮碰撞、多地面类型 |
| 与 RC Plaza 的匹配度 | EV 引擎类型可直接建模无刷电机；多地面类型完全匹配广场砖 / 水泥 / 草地需求 |
| GitHub | https://github.com/sowa705/UDrive |

**适用场景**：如果项目需要更完整的传动系统模拟，UDrive 的 EV 引擎类型值得深入研究。

#### （4）物理 SDK 选型建议

| 方案 | 授权 | 核心优势 | 推荐度 |
|---|---|---|---|
| **TLabVehiclePhysics** | 开源 | Pacejka + LUT、直接满足轮胎模型需求 | ⭐⭐⭐⭐⭐ |
| UDrive | LGPL 3.0 | EV 引擎类型、多地面类型、完整传动系统 | ⭐⭐⭐⭐ |
| CustomWheelCollider | 开源 | 轻量级、悬架实现简洁 | ⭐⭐⭐ |

**推荐组合**：**TLabVehiclePhysics 作为主物理方案**，UDrive 作为传动系统和多地面类型的参考实现。

### 6.3 音频系统（性能优先）

#### （1）VehicleNoiseSynthesizer（VNS，推荐首选）

VNS 是一个开源、免费的多线程异步音频插件，基于发动机 RPM 模拟车辆声音，采用颗粒合成方式，不依赖 FMOD 等外部中间件。

| 项目 | 详情 |
|---|---|
| 授权 | 开源免费 |
| 引擎要求 | Unity 2021.3+，需要 Unity.Mathematics 和 Unity.Burst |
| 核心功能 | 基于 RPM 的实时声音合成、颗粒合成、多线程异步计算、恒定功率交叉淡化 |
| 与 RC Plaza 的匹配度 | 颗粒合成方式适合处理 RC 无刷电机的高频 PWM 啸叫，可在运行时动态生成和变调 |
| 性能特点 | 异步 Burst 编译计算，CPU 开销极低 |
| GitHub | https://github.com/ATG-Simulator/VehicleNoiseSynthesizer |

**注意事项**：VNS 需要为每个 RPM 区间准备独立的音频文件，且需要仔细调整参数。对于 RC 车的电机 PWM 声，可以录制真实的 VXL-3s 电调在不同油门开度下的声音作为音频素材。

#### （2）Unity 原生音频系统

Unity 自带的 AudioSource 组件配合 C# 脚本可以实现基础的音频变调和混合。对于齿轮噪音层和轮胎滚动噪音层，使用 Unity 原生方案即可高效实现。

**性能优化措施**：

- 音频资源按场景打包为 AssetBundle，仅在需要时加载[reference:9]
- 电机层使用 VNS 的异步 Burst 计算，不占用主线程
- 轮胎层使用单一 AudioSource + 材质参数控制，减少 AudioSource 数量

#### （3）音频 SDK 选型建议

| 方案 | 授权 | 核心优势 | 推荐度 |
|---|---|---|---|
| **VNS** | 开源免费 | 颗粒合成、RPM 驱动、多线程异步 | ⭐⭐⭐⭐⭐ |
| Unity 原生 AudioSource + AudioMixer | 免费 | 无额外依赖、轻量 | ⭐⭐⭐⭐ |

**推荐组合**：**VNS 负责电机 PWM 层**，**Unity 原生 AudioSource 负责齿轮层和轮胎层**。

### 6.4 输入系统

Unity Input System 是 Unity 官方的输入系统包，免费使用。

**Windows 平台特别优化**：

- 支持原生键盘输入，无额外延迟
- 输入动作（Action）映射可配置——键盘和手柄的油门 / 转向曲线可独立调校
- 运行时重绑定 UI——玩家可自定义按键
- 为后续添加力反馈方向盘预留接口

### 6.5 Windows 构建性能设置

| 设置项 | 推荐值 | 说明 |
|---|---|---|
| Scripting Backend | **IL2CPP** | 性能优于 Mono，支持链接时间代码生成（LTCG）[reference:10] |
| Api Compatibility Level | .NET Standard 2.1 | 兼顾兼容性与性能 |
| Development Build | **关闭** | 关闭后包体减少约 29%，启动时间缩短 55%[reference:11] |
| Copy PDB Files | **关闭** | 关闭后包体减少约 15MB[reference:12] |
| Compression Method | **LZ4** | 解压速度优于 LZ4HC，适合运行时性能优先场景[reference:13] |
| Fullscreen Mode | **Exclusive Fullscreen** | 相比无边框窗口模式可获得轻微性能提升[reference:14] |
| Graphics API | **Direct3D 11** | Windows 平台最稳定的图形 API，兼容性最佳 |
| DirectStorage | 可选启用 | 加速资源加载，减少 I/O 等待[reference:15] |

**代码优化**：

- 使用 `FixedUpdate()` 处理所有物理相关逻辑（WheelCollider 扭矩、转向、悬架）
- 使用对象池管理轮胎滑痕、灰尘粒子等频繁创建销毁的对象
- 广场场景使用 Terrain 或合并网格，减少 Draw Call[reference:17]
- 静态装饰物（长椅、花坛、灯柱）标记为 Static，启用静态合批

### 6.6 性能预算与目标

| 指标 | 目标值 | 说明 |
|---|---|---|
| 帧率 | 稳定 60 FPS | Windows 平台主流配置 |
| 物理步长 | 100Hz（0.01s） | RC 车加速度极高，需要更高物理频率 |
| 车辆物理开销 | < 0.5ms / 帧 | 4 个 WheelCollider + 1 个 Rigidbody |
| 音频开销 | < 0.3ms / 帧 | VNS 异步计算 + 2–3 个 AudioSource |
| Draw Call | < 500 | 广场场景合并网格 + 静态合批 |
| 内存占用 | < 500MB | 纹理压缩 + AssetBundle 按需加载 |

---

## 七、参数调校建议与验证

在开发过程中，建议按以下顺序调校参数以确保“手感像真车”：

1. **第一步：静态验证**。将 Slash 的质量设为 2.64 kg，在平整地面上以 1 m/s 匀速滑行，观察滑行距离是否与真实 RC 的收油滑行距离量级一致（约 3–6 米后停止）。
2. **第二步：加速验证**。满油门从静止加速，Slash 应在约 2.5–3.5 秒内达到 40 km/h（2S 配置下），Kraton 在 6S 下约 2–3 秒达到 70 km/h。
3. **第三步：转向验证**。以 25 km/h 速度进行定圆转向，Slash 的稳态侧倾角应约 3–5 度，Kraton 应约 5–8 度。横向加速度超过轮胎抓地极限时，应出现渐进的滑移。
4. **第四步：电池验证**。满电起步时动力充沛，行驶 5–8 分钟后，同等油门开度下的加速体感应明显减弱，极速下降 10–15%。

---

## 八、设计约束与诚实边界

**可以做到“物理可信”的部分**：质量、重心转移、悬架行程、轮胎纵向 / 横向力曲线、电机扭矩 - 转速关系、电池电压衰减对动力的影响。

**无法做到“精确还原”的部分**：RC 车的悬挂几何（如滚转中心、抗蹲特性）在简化模型中无法精确建模；轮胎的瞬态响应（如松弛长度）会被忽略；真实 RC 的舵机响应速度（通常 0.06–0.12 秒 / 60°）与键盘输入之间有天然的不匹配。但在正常驾驶范围内，节奏感、重量感、动力感的还原度可以达到“熟悉 RC 的人一上手就知道这是 RC 而不是赛车”的程度。

**关于“参考网络已有车型”的边界**：Traxxas 和 ARRMA 是商业品牌，游戏中使用其车型名称和外观需要获得授权。实际开发时建议使用“风格化但可辨识”的设计——保留短卡 / 大脚的轮廓特征和性能参数，但避免直接复制品牌标识和精确车身线条。

---

## 九、开发路线图

| 阶段 | 内容 | 预估周期 |
|---|---|---|
| P0 — 物理原型 | WheelCollider 车辆 + 两套参数 + 键盘输入 + 广场地面 | 2–3 周 |
| P1 — 手感调校 | 轮胎曲线、扭矩曲线、悬架参数对齐真实 RC 体感 | 2–3 周 |
| P2 — 声音系统 | 三层音频、变调逻辑、地面交叉淡化 | 2 周 |
| P3 — 电池系统 | 电压衰减模型、低压保护、HUD 显示 | 1–2 周 |
| P4 — 场景完善 | 广场细节、过渡地形、装饰物碰撞 | 1–2 周 |
| P5 — 打磨 | 视觉反馈（轮胎滑痕、灰尘）、重置逻辑、车型切换 UI | 2–3 周 |

总计约 **10–14 周** 可完成可玩的完整版本。


## 十、免费技术栈总览（Windows 性能优先）

| 层级 | 技术选型 | 说明 | 成本 |
|---|---|---|---|
| 引擎 | Unity 2022 LTS | Windows Standalone 构建，IL2CPP 后端 | 免费 |
| 语言 | C# | 所有游戏逻辑与物理参数调校 | 免费 |
| 物理轮 | TLabVehiclePhysics | Pacejka + LUT 轮胎模型，开源 | 免费 |
| 物理备选 | UDrive / CustomWheelCollider | 完整传动系统 / 轻量级悬架参考 | 免费 |
| 音频（电机层） | VehicleNoiseSynthesizer (VNS) | 颗粒合成，异步 Burst 计算 | 免费 |
| 音频（齿轮 / 轮胎层） | Unity 原生 AudioSource + AudioMixer | 简单变调和音量映射 | 免费 |
| 输入 | Unity Input System | 官方包，多设备支持 | 免费 |
| 参数配置 | ScriptableObject / JSON | Unity 内置 | 免费 |
| 版本控制 | Git + Git LFS | 管理大体积资源文件 | 免费 |

**总 SDK 成本：$0**。全部技术组件均为开源免费或 Unity 官方免费提供。

**性能优化核心设置清单**：

- Fixed Timestep = 0.01（100Hz）
- Scripting Backend = IL2CPP
- Development Build = 关闭
- Compression = LZ4
- Fullscreen = Exclusive Fullscreen
- Graphics API = Direct3D 11
- 启用 Rigidbody 休眠 + CCD
- 静态装饰物标记 Static + 静态合批

---

## 附录 A：实现刷新记录（2026-09-16）

> 由实现方回写：设计文档正文保持原稿，实现与设计的全部出入、最终参数与
> 实测验收数值统一记录在此，作为"当前有效口径"。工程位于 `D:\RCPlaza`，
> 运行与操作说明见 `README.md`。

### A.1 完成度

§2 车型参数、§3 物理模型（自研 Pacejka + raycast 悬挂 + 三段扭矩曲线 + 电池
sag/LVC）、§4 三层程序化音频、§5 广场场景、§6 性能配置、§8 诚实边界
（风格化命名）均已实现；§9 路线图 P0–P5 全部交付。工程纯文本（零二进制资产、
零 .meta），模型/纹理/音频/UI 全部程序化生成；入口为
`[RuntimeInitializeOnLoadMethod]` 引导，打开工程按 Play 即玩。

### A.2 六处设计修订（与 README §六一一对应）

1. **悬架参数（§3.3）**：文档 k=28000–32000 N/m 下 2.64 kg 车身静压仅
   0.21 mm，与 §3.3 自身"Target Position 0.5"矛盾。修正 = 按"静载压缩 ≈ 50%
   行程"反推：SCT k=370 N/m、c=32 N·s/m；MT k=530 N/m、c=55 N·s/m
   （阻尼 ζ≈0.9 拟临界，压起步俯仰振荡）。
2. **扭矩曲线（§3.4）**：文档曲线在 0 转速处扭矩为 0（不动点，无法起步）。
   改为堵转至 70% 转速 0.92 峰值平台、95–100% 尾段衰减至 0（极速由"风阻 ×
   扭矩衰减"自然平衡产生）；峰值扭矩 SCT 0.100 N·m、MT 0.35 N·m（见 6）。
3. **侧倾验收（§7③）**：文档 3–5° 与 §3.2 CG 参数互斥（逐项代入得 ≈1.3°）。
   验收区间改为 SCT 1.0–3.0°、MT 0.2–1.5°。
4. **极速（§2.1）**：阻力平衡逐项求解 ≈58.5 km/h，74 m 直道实测
   **59.0 km/h**（文档"约 50"为官宣保守值；3S 外推 ≈70 km/h，"80+"需更高
   Kv/齿比，风阻 ∝v²、v∝P^⅓）。
5. **MT 0→70（§7②）**：文档 2–3 s 与 μFz≈31 N 抓地上限互斥（要 3 s 需等效
   μ≈1.1）。解析 ≈4.5 s，实测 **4.78 s**，验收区间 [4.0, 5.5] s。
6. **MT 峰值扭矩**：0.35 N·m = 3.5× SCT（§3.4 指引 2.5–3×），反推自 §7②
   与 §5 路沿爬升验收。

### A.3 技术选型落地（替代 §6.2/6.3/6.4 所列 SDK）

| 设计选型 | 实际实现 | 理由 |
|---|---|---|
| TLabVehiclePhysics（§6.2） | 自研 Pacejka + raycast 悬挂（`Scripts/Core/`、`Scripts/Sim/`） | 零依赖、与 §3 公式逐项对应；规避第三方 SDK 的 URP 依赖与参数黑盒 |
| VehicleNoiseSynthesizer（§6.3） | `AudioClip.Create` 程序合成（噪声/谐波/滤波变调） | 零素材零包依赖；5 个 AudioSource 三层叠加 + 0.2s 恒定功率交叉淡化（§4） |
| Unity Input System（§6.4） | `Input.GetKey`（`Scripts/Core/RcInput.cs`） | 键位面板固定，免除输入包与 Action 配置 |
| IL2CPP/LZ4/独占全屏（§6.5） | 打包期选项（编辑器运行不受影响） | 构建 exe 步骤见 README §八 |

### A.4 如何运行测试与如何正式启动

- **自动化验收测试**（一条命令；退出码 0=全部通过、1=许可证问题、2=有失败。
  覆盖 §7 全部验收项及 §4/§5/§6 配置核对，[TEST] 行打印实测数值）：

  ```powershell
  powershell -ExecutionPolicy Bypass -File scripts\RunTests.ps1
  ```

- **正式启动游戏（图形界面）**（`scripts\OpenGame.ps1`：自动定位编辑器、
  确保 Hub 存活、载入游戏场景；编辑器窗口内按 ▶ Play 即玩，`-Stop` 停止）：

  ```powershell
  powershell -ExecutionPolicy Bypass -File scripts\OpenGame.ps1
  ```

- 键位与 F 键内置验收工具（F2 调试面板 / F3 滑行测试 / F4 加速计时）见
  README §二。

### A.5 实测验收数值（2026-09-16 全量回归，退出码 0）

| §7 验收项 | 实测 | 验收区间 |
|---|---|---|
| ① 1 m/s 收油滑行 | 5.18 m | 3–6 m |
| ② SCT 0→40 km/h | 2.85 s | 2.5–3.5 s |
| ② MT 0→70 km/h | 4.78 s | 4.0–5.5 s（修订见 A.2·5） |
| ③ SCT 25 km/h 定圆侧倾 | 1.54° | 1.0–3.0°（修订见 A.2·3） |
| ③ MT 25 km/h 定圆侧倾 | 0.90° | 0.2–1.5° |
| 极速（SCT，2S 满电，74 m 直道跑满） | 59.0 km/h | 52–62 km/h（修订见 A.2·4） |
| ④ 电池衰减 | 满电 vs SOC12% 的 0→30 km/h 用时比 2.86 | ≥1.45，且 LVC 限功/切断就位 |
| §5 MT 过 4.5 cm 路沿 | 通过 | 必须通过 |

> §9 P5 的"轮胎滑痕/灰尘粒子"未实现（性能优先版裁剪为程序化反馈：车体姿态、
> 悬挂动作、转速与音频表达），其余 P5 项（重置逻辑、车型切换 UI）已交付。